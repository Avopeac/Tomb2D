#pragma once

namespace core {

	enum class GuiElementType
	{
		Container,
		Leaf,
	};

	enum class GuiHorizontalAnchorPoint
	{
		Left,
		Center,
		Right,
	};

	enum class GuiVerticalAnchorPoint
	{
		Top,
		Middle,
		Bottom,
	};
}
